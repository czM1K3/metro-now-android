query Departures ($platformIds: [ID!]!) {
    departures(platformIds: $platformIds) {
        headsign
        departureTime {
            predicted
        }
        platform {
            id
        }
        route {
            name
            isSubstitute
            vehicleType
            isNight
        }
    }
}
